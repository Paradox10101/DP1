import { atom } from 'jotai';

export const blockageRoutesAtom = atom({});

export const vehicleCurrentRoutesAtom = atom({});

export const showBlockagesRoutesAtom = atom(true);

export const showVehiclesRoutesAtom = atom(true);